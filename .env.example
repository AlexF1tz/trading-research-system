# The deterministic sample does not require credentials.
MARKET_DATA_PROVIDER=synthetic_fixture

# Stage 2 historical-quality command. Set these only in your local environment
# or ignored .env file; never commit values. The code uses market-data GETs only.
ALPACA_API_KEY_ID=
ALPACA_API_SECRET_KEY=

# Unimplemented optional adapters. Leave blank.
MASSIVE_API_KEY=
SEC_USER_AGENT=
